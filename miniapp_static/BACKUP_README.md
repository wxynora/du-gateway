本目录由 Mini App 前端构建产物生成。

为避免“改代码前先备份”的要求落空，历史上手写过的 `index.html` / `app.js` 已备份到：
- `miniapp_static/_backup/index.html`
- `miniapp_static/_backup/app.js`

后续请在 `miniapp/` 前端工程内开发，使用 `npm run build` 输出到本目录。
