# 文游开源规则入口

这个文件只保留规则总览和文档导航。具体规则拆到 `docs/wenyou/` 下，方便后续开源、接不同后端和按模块维护。

## 目标

- 做一个高自由度无限流文字游戏规则集。
- 不绑定当前后端；后端只负责执行结构化规则、结算数值和保存状态。
- DS/GM 负责叙事表达，不直接判定精准数值、抽卡概率、商店价格或道具效果。
- 规则固定后，可以由任意后端实现 ruleset/content pack。

## 文档地图

| 文档 | 内容 |
| --- | --- |
| [`docs/wenyou/core_loop.md`](wenyou/core_loop.md) | 目标、术语、状态机、整备空间边界、任务者编制、公开/隐藏信息、副本类型 |
| [`docs/wenyou/runtime_state.md`](wenyou/runtime_state.md) | 副本运行时缓存、状态分层、每轮行动流程、GM 输入输出边界、面板读取规则 |
| [`docs/wenyou/instance_generation.md`](wenyou/instance_generation.md) | 副本蓝图、生成顺序、怪物生态结构 |
| [`docs/wenyou/monster_system.md`](wenyou/monster_system.md) | 怪物生成预算、普通怪/精英怪/Boss 面板、战斗/规避结算、Boss 不可硬杀规则 |
| [`docs/wenyou/numeric_growth.md`](wenyou/numeric_growth.md) | 六基础属性、当前精神力、行动判定、状态效果、复活债务、Lv1-30 成长曲线、属性点、晋升、能力 |
| [`docs/wenyou/item_ability_system.md`](wenyou/item_ability_system.md) | 道具目录、分类、SQL 表、商店、抽卡、背包物品、耐久、回收、核心能力系统 |
| [`docs/wenyou/rewards_economy.md`](wenyou/rewards_economy.md) | 通关奖励、评级、奖励 roll、掉落表、经济平衡、强制惩罚副本、系统打工 NPC 模式 |
| [`docs/wenyou/backend_contracts.md`](wenyou/backend_contracts.md) | 默认 Schema、State Patch、后端接口建议、内容包建议、当前项目迁移备注 |
| [`docs/wenyou/implementation_checklist.md`](wenyou/implementation_checklist.md) | 功能实现验收清单、规则测试用例、开源前文档收束项 |

## 硬规则摘要

- 任务者总数为 2-13 名，包含玩家和 NPC 任务者；玩家数量不固定。
- NPC 任务者是无限流游戏里的其他“玩家”，不是真人联机用户。
- 任务者之间默认不能直接致死，但可以欺骗、误导、抢资源、设局或阴人。
- NPC 任务者不强制做关系值；最小规则只记录立场、当前意图、是否会使坏和存活状态。
- 每个副本在生成具体剧情前，必须先生成简短 `instance_blueprint`，列出主线、支线、隐藏结局、关键线索、NPC 任务者和威胁时钟。
- 副本正式生成后，后端必须缓存运行时状态；任务进度、线索、位置、状态、威胁时钟、背包和怪物实例都由后端按规则更新，不能要求 DS 每轮重写。
- DS/GM 每轮只输出剧情、事件意图和状态建议；最终状态变化必须由后端写入 `state_patch`。
- 通关评级只结算真实玩家控制的玩家角色/玩家队伍；NPC 任务者不参与玩家评级，除非 NPC 相关目标被写入该玩家的支线、隐藏支线、隐藏结局或特殊成就。
- 通关评级按主线、普通支线、隐藏支线、隐藏结局、特殊成就和损耗记录结算；线索不作为独立评分项，只作为目标完成证据。
- 怪物生态由后台随副本生成：普通怪、精英怪和 Boss；Boss 默认不可正面战胜，通常需要封印、削弱、规避或达成撤离条件。
- 六基础属性为力量、体力、敏捷、智力、精神力、幸运；精神力可加点，当前精神力会随 SAN 损耗下降。
- 默认等级上限为 Lv30；新手副本通关礼包固定发 6 点自由属性点，普通升级每级发 3 点自由属性点。
- 幸运只影响副本内随机、隐藏支线发现和结算掉落小幅加权；不影响付费抽卡概率、十连保底、100 抽大保底、卡池权重和抽卡价格。
- 失败默认不给积分；死亡、复活、治疗、债务和污染构成失败成本。
- 抽卡单抽 100 积分，100 抽大保底必出随机 S/传说；抽卡接口不能读取幸运相关字段。
- 普通商店每日轮转 7-8 个物品；随玩家阶位提高，可以低概率出现越级物品，但高阶效果仍按 `rank_min`、`seal_rank`、限购和库存判断，不足阶位时封印或降级生效。
- S/传说级道具若在普通商店低概率出现，价格必须高于 100 抽随机 S 大保底成本，默认不低于 12000 积分。
- 背包道具和核心能力的精准效果由后端规则引擎判定，GM 只收到判定结果。
- 自由文本行动必须先归类为固定 `action_type`；玩家或 GM 文本声明“已经成功”不等于规则成功。
- 惩罚副本可以是“替系统打工”的 NPC 工单：玩家进入其他副本扮演 NPC，不能暴露给任务者或怪物阵营。

## 当前项目关系

当前仓库里的文游实现仍是测试载体。规则文档按开源版设计，不绑定当前后端；等规则和体验测试稳定后，可以复制一份到新仓库作为独立项目。

现阶段不要把这些文档当成已全部实现；具体功能以 `backend_contracts.md` 的实现对齐表和 `implementation_checklist.md` 的验收项为准。功能做完后要收束旧“缺口/草稿”描述，保留规则契约和内容包说明。
