# 查看上游原始响应

需要核对上游真实返回结构时，临时把 `.env` 中的日志级别改为：

```env
LOG_LEVEL=DEBUG
```

重启本次涉及的网关进程后发一条请求，日志会显示上游响应的顶层字段、message 字段、正文预览和受限长度的结构样本。

这里的“原始响应”是网关收到后、进入正文拆分、隐藏标记、渠道发送等后处理之前的响应；日志仍会按实现对长度和敏感字段做限制。

调试完成后恢复：

```env
LOG_LEVEL=INFO
```

并重启对应进程，避免 DEBUG 日志持续增加磁盘写入。
