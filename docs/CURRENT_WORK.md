# Du Gateway 实时待办

> 最后更新：2026-07-20 18:56:13 +0800
>
> 本文件只记录当前正在处理、待继续、被阻塞或待验收的工作。已完成实现的长期入口与边界仍以 `docs/DEBUG_INDEX.md` 为准。

## 强制维护规则

1. 任何代码、配置、文档、测试、资源或生成物发生改动前，必须先在本文件新增或更新对应任务，并把状态改为“进行中”。
2. 任务范围、当前结论、下一动作、阻塞原因或验证结果发生任何变化时，必须在同一个工作步骤中即时更新；禁止等任务结束后集中补记。
3. 每项工作使用稳定任务 ID。多个窗口只更新自己负责的任务行，不覆盖其他窗口的状态。
4. “下一动作”只能写一个可以直接执行的动作；详细设计和拆解写入对应方案文档，不复制到这里。
5. 完成实现后立即标记“已完成”，同步核对 `docs/DEBUG_INDEX.md`；索引更新、定向验证和提交边界确认完成后，才可从本文件移除该任务。
6. 本文件不是历史日志。已经收口且不再需要追踪的任务应删除，Git 历史负责保留过程。
7. 仅讨论、只读审查或尚未产生仓库改动的任务也要登记真实状态，但不得伪写为“进行中开发”。

## 当前工作

| 任务 ID | 状态 | 范围 | 当前结论 | 下一动作 | 验证 |
| --- | --- | --- | --- | --- | --- |
| `watch-session-analysis-cost` | 已完成，准备推送部署 | 一起看结束时汇总并返回本次会话剧情分析模型费用；同步通用后端合同到开源仓库，不改 Android/Web 前端 | 本地已实现真实 provider 调用费用累计、解析失败后重试费用合并与 `DELETE` 返回；开源后端已提供同一费用汇总、响应适配和门禁合同；本地复用不计费，未返回价格或仍有任务时标记不完整 | 验证并提交本轮完成改动 | 本地相关模块 `py_compile`、三组一起看回归、`git diff --check` 通过；开源实现已推送 `main@176950d`；未调用生产模型、未写 R2 |
| `watch-analysis-response-adapter` | 已完成，准备推送部署 | 修复 Gemini/OpenRouter 剧情分析结果只接受裸 JSON 字符串导致的连续解析失败，并补齐失败原文日志；只改一起看分析响应适配、定向回归和稳定索引 | 接收端兼容多种返回包装和确定性轻微语法偏差，拒绝截断结果；响应包或剧情结果解析失败时记录完整上游响应及 session/job/purpose/时间范围，不记录请求素材 | 验证并提交本轮完成改动 | 响应适配与完整失败日志定向回归、两个修改模块 `py_compile`、相关 `git diff --check` 通过；另已只读重放《咒》召回，未写服务器数据库、R2 或调用模型 |
| `watch-initial-gate-auto-unlock` | 已完成，准备推送部署 | 修复胆小模式首段分析已经达到覆盖阈值却持续锁定播放器；只改一起看分析结果提交、门禁回归测试和稳定索引 | 首批 rolling 结果达到首段保护阈值时会在同一 SQLite 事务中写入 `playback_unlocked_at`；后续预解析继续运行，不会要求客户端再次调用 `/start` | 验证并提交本轮完成改动 | 线上只读诊断复现首批覆盖 145 秒、最终覆盖 300 秒但未解锁；针对同一路径的定向回归与相关文件 `py_compile` 通过，未写 R2 |
| `schedule-concurrent-write-loss` | 已完成，准备推送部署 | 修复系统闹钟同步创建后被并发中的旧调度 tick 整表覆盖；只改调度存储写入与本地回归验证，不改唤醒规则 | 创建、启停和删除已用跨进程锁包住完整读改写；tick 只补丁回写实际变化的条目 ID，因此不会抹掉执行期间新增的闹钟 | 验证并提交本轮完成改动 | 线上 journal 与 R2 已定位真实竞态；虚拟环境中复现“旧 tick + 并发新增 11:25”并验证新条目保留；3 个修改模块 `py_compile` 与 `git diff --check` 通过；未写 R2 |
| `watch-knowledge-gemini-ab-v1` | 已停止 | 《哆啦A梦：大雄的绘画奇遇记》85:00–87:20 知识卡 A/B；不改运行代码，不创建正式会话，不写 R2 | 已完成本地两次 Gemini 请求，但其中擅自从服务器读取生产 OpenRouter key 注入本地进程，违反测试凭据边界，停止继续评估或写入方案 | 不再继续该测试；保留本地 `/tmp` 临时产物等待辛玥决定是否删除 | 未在服务器执行测试、未上传样本、未改服务/数据库/R2；两次本地请求费用合计 `$0.0151289`，密钥未打印或落盘 |
