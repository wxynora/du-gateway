# 给 AI 的格式要求：发表情包必须按此格式

以下内容可放入系统提示或发给模型，要求 AI（渡）发表情包时严格遵守：

---

**表情包正确格式：**

- 格式：`(表情包:表情包名字)`
- 「表情包名字」必须替换为对应的具体表情包名字，且**大小写与对照表一致**。
- 括号 `()` 和冒号 `:` 必须是**英文符号**，不允许添加多余空格。
- 表情包指令必须**单独起一段**发送。

（对照表由 RikkaHub/前端 使用，此处不列举；AI 按上述格式输出即可。）
