{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Wenyou Core Ability Archetype Catalog",
  "type": "object",
  "required": ["schema_version", "abilities"],
  "properties": {
    "schema_version": { "type": "integer" },
    "source": { "type": "string" },
    "abilities": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "rarity", "desc"],
        "properties": {
          "id": { "type": "string" },
          "name": { "type": "string" },
          "rarity": { "enum": ["D", "C", "B", "A", "S"] },
          "rank_min": { "enum": ["D", "C", "B", "A", "S"] },
          "uses_per_instance": { "type": "integer", "minimum": 1 },
          "desc": { "type": "string" },
          "tags": { "type": "array", "items": { "type": "string" } },
          "source_tags": { "type": "array", "items": { "type": "string" } },
          "effect_json": { "type": "object" }
        }
      }
    }
  }
}
